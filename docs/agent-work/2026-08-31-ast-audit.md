# AST repo audit — 2026-08-31

Agent: Lead Blockchain Developer  
Repo: this tree.

Tests were not run in full this slice (`npm test` not recorded).

## Present (non-empty)

| Path | What |
|------|------|
| `src/nodechain/` | journal, rocksdb/file/memory store, specs |
| `src/pot/` | attestation, quorum, criteria, validator-registry + specs |
| `src/emission/` | mint only after journal ok-to-emit (PoT verified) |
| `src/aroscoin/` | coin service |
| `rust/crates/` | `nodechain-journal`, `pot-types` |
| `portal/` | edge; portal never mints |
| `contracts/` | Foundry `ArosCoinView` (representation, not SoT) |

## Ratified

Asset token is **permanent** (2026-08-31). Do not extinguish on process close. See Core Canon Amendment 1.

## Next

Record `npm test` pass/fail in a follow-up file in this folder.
